[project]
name = "home-erp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Diego Rivera", email = "diego.rivera.estefano@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
home-erp = "home_erp:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
